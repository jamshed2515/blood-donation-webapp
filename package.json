{
  "name": "liforcecopy",
  "version": "1.0.0",
  "description": "Blood Bank Donation System is a php based web project with both admin and user layouts.",
  "main": "index.js",
  "scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "build": "npx tailwindcss build assets/styles.css -o public/styles.css",
  "dev": "npx tailwindcss -i ./assets/styles.css -o ./public/styles.css --watch"
},

  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.1.3"
  }
}
